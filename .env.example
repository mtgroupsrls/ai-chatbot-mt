# Generate a random secret: https://generate-secret.vercel.app/32 or `openssl rand -base64 32`
AUTH_SECRET=****

# Azure OpenAI Configuration
# Get these from your Azure OpenAI resource in the Azure Portal
# https://portal.azure.com
AZURE_RESOURCE_NAME=****
AZURE_API_KEY=****

# Azure OpenAI Deployment Names (optional - defaults to gpt-4o/gpt-4o-mini)
# These should match the deployment names you created in Azure OpenAI Studio
AZURE_DEPLOYMENT_NAME=gpt-4o
AZURE_DEPLOYMENT_NAME_REASONING=gpt-4o
AZURE_DEPLOYMENT_NAME_TITLE=gpt-4o-mini
AZURE_DEPLOYMENT_NAME_ARTIFACT=gpt-4o


# PostgreSQL Database Connection String
# Format: postgresql://[user[:password]@][host][:port][/dbname][?param1=value1&...]
# Example: postgresql://username:password@localhost:5432/chatbot_db
POSTGRES_URL=postgresql://username:password@localhost:5432/chatbot_db


# Instructions to create a Redis store here:
# https://vercel.com/docs/redis
REDIS_URL=****
